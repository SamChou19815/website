version: 2.1
jobs:
  code-quality-check:
    description: Run code quality checks
    docker:
      - image: circleci/node:lts
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - yarn-dependencies-{{ .Branch }}-{{ checksum "yarn.lock" }}
            - yarn-dependencies-{{ .Branch }}-
            - yarn-dependencies-
      - run: yarn install
      - save_cache:
          paths:
            - node_modules
          key: yarn-dependencies-{{ .Branch }}-{{ checksum "yarn.lock" }}
      # Check sam-highlighter
      - run: yarn workspace sam-highlighter tsc
      - run: yarn workspace sam-highlighter build
      # Check main-site-frontend
      - run: yarn workspace main-site-frontend tsc
      - run: yarn workspace main-site-frontend lint
      - run: yarn workspace main-site-frontend build
      # Check samlang-demo-frontend:
      - run: yarn workspace samlang-demo-frontend tsc
      - run: yarn workspace samlang-demo-frontend lint
      - run: yarn workspace samlang-demo-frontend build
      # Check ten-web-frontend:
      - run: yarn workspace ten-web-frontend tsc
      - run: yarn workspace ten-web-frontend lint
      - run: yarn workspace ten-web-frontend build
      # Check blog:
      - run: yarn workspace blog build
  deploy-changed-frontend:
    description: Deploy changed frontend directory to firebase hosting
    docker:
      - image: circleci/node:lts-browsers
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Deploy to Firebase
          command: |
            if [ "${CIRCLE_BRANCH}" = "master" ]; then
              ./ci-deploy
            else
              echo "Not master branch, do not deploy"
            fi
workflows:
  version: 2
  check-and-deploy-frontend:
    jobs:
      - code-quality-check
      - deploy-changed-frontend:
          requires:
            - code-quality-check

{
  "name": "@dev-sam/repo-tools",
  "description": "Developer Sam's repository tools.",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "run -T tsc",
    "compile": "yarn build && NODE_NO_WARNINGS=1 ncc build src/index.ts -o bin -m && git status --porcelain"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@types/node": "^14.0.14",
    "@zeit/ncc": "^0.22.3"
  }
}

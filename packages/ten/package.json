{
  "name": "ten",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.9.14",
    "lib-react": "0.1.0",
    "next": "^9.4.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-test-renderer": "^16.13.1"
  },
  "scripts": {
    "start": "NODE_NO_WARNINGS=1 next",
    "build": "NODE_NO_WARNINGS=1 next build && next export",
    "compile": "run -T tsc",
    "deploy": "yarn common:firebase deploy --token=$FIREBASE_TOKEN --non-interactive --only hosting:ten"
  },
  "proxy": "https://us-central1-dev-sam.cloudfunctions.net/HandleTenAIMoveRequest",
  "devDependencies": {
    "@types/jest": "^25.2.3"
  }
}

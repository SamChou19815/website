{
  "name": "vault",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_NO_WARNINGS=1 docusaurus start",
    "build:private-docs-generator": "run -T tsc private-docs-generator.ts --incremental --tsBuildInfoFile private-docs-generator.tsbuildinfo",
    "build:private-docs": "yarn build:private-docs-generator && node private-docs-generator.js",
    "build:public": "NODE_NO_WARNINGS=1 docusaurus build",
    "build:private": "yarn build:private-docs && DEV_SAM=true NODE_NO_WARNINGS=1 docusaurus build --out-dir deployment-template/build",
    "build": "yarn build:public && yarn build:private",
    "swizzle": "NODE_NO_WARNINGS=1 docusaurus swizzle",
    "compile": "yarn build",
    "deploy:private": "cd deployment-template && gcloud app deploy",
    "deploy": "yarn deploy:private"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.56",
    "@docusaurus/plugin-google-analytics": "^2.0.0-alpha.56",
    "@docusaurus/plugin-google-gtag": "^2.0.0-alpha.56",
    "@docusaurus/preset-classic": "^2.0.0-alpha.56",
    "@mdx-js/react": "^1.6.4",
    "classnames": "^2.2.6",
    "lib-docusaurus-plugin": "0.1.0",
    "lib-react": "0.1.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.19"
  },
  "devSamRepositoryDependencies": [
    "private-monorepo"
  ],
  "deploymentDependencies": ["gcloud"],
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

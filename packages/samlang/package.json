{
  "name": "samlang",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "compile": "yarn build",
    "test": "echo 'No tests yet.'",
    "deploy": "node ../../node_modules/.bin/firebase deploy --token=$FIREBASE_TOKEN --non-interactive --only hosting:samlang"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.55",
    "@docusaurus/preset-classic": "^2.0.0-alpha.55",
    "classnames": "^2.2.6",
    "lib-docusaurus-prism-extended-plugin": "0.1.0",
    "lib-react": "0.1.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

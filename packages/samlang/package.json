{
  "name": "samlang",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "esbuild-scripts start",
    "build": "esbuild-scripts ssg",
    "check": "tsc --noEmit --incremental",
    "deploy": "netlify deploy -d build -s samlang.io --prod"
  },
  "dependencies": {
    "@dev-sam/samlang-core": "^0.6.0",
    "@monaco-editor/react": "^4.4.5",
    "lib-react-prism": "workspace:*",
    "monaco-editor": "^0.33.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "esbuild-scripts": "workspace:*"
  }
}

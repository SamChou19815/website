{
  "name": "samlang",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "esbuild-scripts start",
    "build": "esbuild-scripts ssg",
    "compile": "tsc --noEmit --incremental",
    "deploy": "netlify deploy -d build -s samlang.io --prod"
  },
  "dependencies": {
    "@dev-sam/samlang-core": "^0.6.0",
    "@monaco-editor/react": "^4.3.1",
    "autosize": "^5.0.1",
    "lib-react-prism": "0.1.0",
    "monaco-editor": "^0.33.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "esbuild-scripts": "workspace:0.6.0"
  }
}

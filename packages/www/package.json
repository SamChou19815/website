{
  "name": "www",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "esbuild-scripts start",
    "build": "esbuild-scripts ssg",
    "check": "tsc --incremental",
    "deploy": "netlify deploy -d build -s developersam.com --prod"
  },
  "dependencies": {
    "lib-react-prism": "workspace:*",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "esbuild-scripts": "workspace:*"
  }
}

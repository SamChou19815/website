{
  "name": "blog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/generator/blog-generator.mjs start",
    "build": "node src/generator/blog-generator.mjs build",
    "check": "tsc --incremental",
    "deploy": "netlify deploy -d build -s blog.developersam.com --prod"
  },
  "dependencies": {
    "lib-react-prism": "workspace:*",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "esbuild-scripts": "workspace:*"
  }
}

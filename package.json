{
  "private": true,
  "packageManager": "pnpm@7.15.0",
  "scripts": {
    "check": "turbo run check",
    "build": "turbo run build",
    "cp-dist": "rm -rf dist && mkdir dist && pnpm cp-dist:blog && pnpm cp-dist:samlang && pnpm cp-dist:www",
    "cp-dist:blog": "cp -r packages/blog/build dist/blog",
    "cp-dist:samlang": "cp -r packages/samlang/build dist/samlang",
    "cp-dist:www": "cp -r packages/www/build dist/www",
    "format": "rome format --write .",
    "lint": "rome check .",
    "bump": "pnpm up -r --latest @types/node @types/react @types/react-dom typescript husky lint-staged esbuild"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.11.11",
    "@types/react": "^18.0.26",
    "esbuild": "^0.16.1",
    "husky": "^8.0.2",
    "lint-staged": "^13.1.0",
    "postcss": "^8.4.19",
    "rome": "^11.0.0",
    "tailwindcss": "^3.2.4",
    "turbo": "^1.6.3",
    "typescript": "^4.9.3"
  },
  "lint-staged": {
    "**/*.{ts,js,tsx,jsx,html,yml}": [
      "pnpm rome format --write"
    ]
  }
}

# @generated

name: CI lib-common
on:
  push:
    paths:
      - packages/lib-common/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-lib-common.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace lib-common compile
      - name: Test
        run: yarn workspace lib-common test

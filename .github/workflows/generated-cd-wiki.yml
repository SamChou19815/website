# @generated

name: CD wiki
on:
  push:
    paths:
      - 'packages/lib-react-loading/**'
      - 'packages/lib-firebase/**'
      - 'packages/lib-react-prism/**'
      - 'packages/esbuild-scripts/**'
      - 'packages/lib-react-docs/**'
      - 'packages/wiki/**'
      - 'configuration/**'
      - '.github/workflows/generated-*-wiki.yml'
    branches:
      - main
env:
  NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: '2'
      - uses: actions/setup-node@v2
        with:
          cache: 'yarn'
      - name: Yarn Install
        run: yarn install --immutable
      - name: Build
        run: yarn workspace wiki build
      - name: Deploy
        run: yarn workspace wiki deploy

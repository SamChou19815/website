# @generated

name: cd-samlang-docs
on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/cd-workflow-samlang-docs.yml
      - package.json
      - 'configuration/**'
      - 'tooling/**'
      - 'sam-highlighter/**'
      - 'samlang-docs/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Set up Node
        uses: actions/setup-node@v1
      - name: Yarn Install
        run: yarn install

      - name: Build dependency sam-highlighter
        run: yarn workspace sam-highlighter build
      - name: Build samlang-docs
        run: yarn workspace samlang-docs build

      - name: Build & Deploy samlang-docs
        env:
          DEPLOY_GH_PAGE_TOKEN: ${{ secrets.DEPLOY_GH_PAGE_TOKEN }}
        run: |
          git config --global user.name "SamChou19815"
          git config --global user.email "sam@developersam.com"
          GIT_USER=$DEPLOY_GH_PAGE_TOKEN yarn workspace samlang-docs deploy

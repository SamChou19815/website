# @generated

name: CI lib-docusaurus-prism-extended-plugin
on:
  push:
    paths:
      - packages/lib-docusaurus-prism-extended-plugin/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-lib-docusaurus-prism-extended-plugin.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - uses: actions/cache@v1
        with:
          path: ~/.yarn/berry/cache
          key: yarn-berry-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            yarn-berry-
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace lib-docusaurus-prism-extended-plugin compile
      - name: Test
        run: yarn workspace lib-docusaurus-prism-extended-plugin test

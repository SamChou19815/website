# @generated

name: CI lib-react
on:
  push:
    paths:
      - packages/lib-react/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-lib-react.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace lib-react compile
      - name: Test
        run: yarn workspace lib-react test

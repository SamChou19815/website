# @generated

name: CI queue
on:
  push:
    paths:
      - packages/lib-common/**
      - packages/lib-firebase/**
      - packages/lib-prism-extended/**
      - packages/lib-react/**
      - packages/queue/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-queue.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - uses: actions/cache@v1
        with:
          path: ~/.yarn/berry/cache
          key: yarn-berry-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            yarn-berry-
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace queue compile
      - name: Test
        run: yarn workspace queue test

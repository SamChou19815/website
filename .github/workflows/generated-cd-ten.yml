# @generated

name: CD ten
on:
  push:
    branches:
      - master
    paths:
      - packages/lib-react/**
      - packages/ten/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-ten.yml
env:
  FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Use Yarn Cache
        uses: actions/cache@v1
        with:
          path: ~/.cache/yarn
          key: yarn-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: yarn-
      - name: Yarn Install
        run: yarn install
      - name: Build
        run: yarn workspace ten build
      - name: Deploy
        run: yarn workspace ten deploy

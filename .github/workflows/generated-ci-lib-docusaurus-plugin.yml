# @generated

name: CI lib-docusaurus-plugin
on:
  push:
    paths:
      - 'packages/lib-prism-extended/**'
      - 'packages/lib-docusaurus-plugin/**'
      - 'package.json'
      - 'yarn.lock'
      - 'configuration/**'
      - '.github/workflows/generated-*-lib-docusaurus-plugin.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - uses: actions/cache@v2
        with:
          path: |
            .yarn/cache
            .pnp.js
          key: yarn-berry-${{ hashFiles('**/yarn.lock') }}
          restore-keys: yarn-berry-
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace lib-docusaurus-plugin compile

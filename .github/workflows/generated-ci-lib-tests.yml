# @generated

name: CI lib-tests
on:
  push:
    paths:
      - packages/lib-common/**
      - packages/lib-prism-extended/**
      - packages/lib-react/**
      - packages/lib-tests/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-lib-tests.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - name: Yarn Install
        run: yarn install
      - name: Compile
        run: yarn workspace lib-tests compile
      - name: Test
        run: yarn workspace lib-tests test

# @generated

name: CD tasks
on:
  push:
    branches:
      - master
    paths:
      - packages/lib-common/**
      - packages/lib-firebase/**
      - packages/lib-react/**
      - packages/tasks/**
      - package.json
      - yarn.lock
      - configuration/**
      - .github/workflows/generated-*-tasks.yml
env:
  FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node
        uses: actions/setup-node@v1
      - name: Use Yarn Cache
        uses: actions/cache@v1
        with:
          path: ~/.cache/yarn
          key: yarn-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: yarn-
      - name: Yarn Install
        run: yarn install
      - name: Build
        run: yarn workspace tasks build
      - name: Deploy
        run: yarn workspace tasks deploy

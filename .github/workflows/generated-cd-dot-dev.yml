# @generated

name: CD dot-dev
on:
  push:
    paths:
      - 'packages/lib-theme-switcher/**'
      - 'packages/lib-colorful-terminal/**'
      - 'packages/esbuild-scripts/**'
      - 'packages/dot-dev/**'
      - 'configuration/**'
      - '.github/workflows/generated-*-dot-dev.yml'
    branches:
      - main
env:
  NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: '2'
      - uses: actions/setup-node@v2
        with:
          cache: 'yarn'
      - name: Yarn Install
        run: yarn install --immutable
      - name: Build
        run: yarn workspace dot-dev build
      - name: Deploy
        run: yarn workspace dot-dev deploy
